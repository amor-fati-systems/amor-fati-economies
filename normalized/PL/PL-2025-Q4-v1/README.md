# Normalized PL-2025-Q4-v1 inputs

These TSVs are deterministic intermediate outputs for the `PL-2025-Q4-v1`
baseline. They are versioned separately from the final bundle so a later
baseline can coexist without overwriting or silently reusing these transforms.
The final loader input is published under `artifacts/PL/PL-2025-Q4-v1/`.
